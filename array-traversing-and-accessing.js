let arr=[3,5,8,2,1,9,0,56,43,22]

// traverse through array elements

// I=0  is the element from where operation array will start
//i<arr.length is the number of operation perform on array elements after that operation will stop 
// i++ is the logic we want to perform on each element of array
for (let i=0; i<arr.length; i++){
  // console.log(arr[i]);
}

// Accessing array elements

let index=5
console.log(arr[index])